# 介绍

Canvas Vue Table 是一个专为 Vue 3 设计的高性能虚拟列表组件库，使用 Canvas 进行渲染，适合表格、列表等海量数据展示场景。

## 特性

- **Canvas 渲染**：不依赖 DOM，渲染性能优异
- **虚拟滚动**：仅渲染可视区域内容，支持百万级数据
- **Vue 3**：Composition API、TypeScript 支持
- **轻量**：无额外重型依赖
